# Renombrá este archivo a .env.local

# Webhook URL del workflow de n8n que recibe los mensajes del chat.
# El endpoint debe responder con JSON: { "reply": "texto" } o { "output": "texto" }.
N8N_WEBHOOK_URL=

# Opcional: si tu webhook requiere autenticación Bearer token.
N8N_WEBHOOK_TOKEN=
